import React, { Component } from 'react'

class Sidebar extends Component {
  render () {
    return (
      <section>
        <button>RUN</button>
      </section>
    );
  }
}

export default Sidebar;
